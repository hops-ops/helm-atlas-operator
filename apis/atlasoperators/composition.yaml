apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: atlasoperators.helm.hops.ops.com.ai
spec:
  compositeTypeRef:
    apiVersion: helm.hops.ops.com.ai/v1alpha1
    kind: AtlasOperator
  mode: Pipeline
  pipeline:
    - step: render
      functionRef:
        name: hops-ops-helm-atlas-operatorrender
      input:
        apiVersion: pt.fn.crossplane.io/v1beta1
        kind: Resources
